apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-a
  namespace: project-a
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web-a
  template:
    metadata:
      labels:
        app: web-a
    spec:
      containers:
      - name: web
        image: public.ecr.aws/d0z6a9w2/project-a:latest
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: ecr-secret
